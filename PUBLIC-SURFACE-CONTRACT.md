# Public Surface Contract

Every production surface declares:

- owning repository;
- exact deployed commit;
- Brand OS version;
- favicon and OG asset source;
- screenshot provenance status;
- last live-verification timestamp.

The organization-wide observed registry lives in `Aftergraph/.github/PUBLIC-SURFACES-REGISTRY.json`; this repository owns only its schema and canonical assets.

